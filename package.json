{
  "name": "exec-log-root",
  "scripts": {
    "build": "yarn workspace exec-log build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^17.0.36",
    "typescript": "^4.7.2"
  }
}

{
  "name": "exec-log-root",
  "version": "0.0.0",
  "scripts": {
    "build": "yarn workspace exec-log build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^17.0.36",
    "typescript": "^4.7.2"
  }
}
